let onSaleInferred = true

let nameInferred = "Whoopie Cushion"

let priceInferred = 4.99

if onSaleInferred {
    println("\(nameInferred) on sale for \(priceInferred)!");
} else {
    println("\(nameInferred) at regular price: \(priceInferred)!")
}